type count is integer;

routine announce(value : count);

routine relay(value : count) is
	announce(value);
end

routine announce(value : count) is
	print value;
end

routine main() is
	var number : count is 8;
	relay(number);
end
